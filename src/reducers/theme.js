
var objectAssign = require('object-assign')


const initialState = {
  COLOR_MAIN: '#20C753',
  HEADER_HEIGHT: 50,
  MAX_WIDTH: 1100,
  SEARCH_WIDTH: 500,
  SEARCH_PADDING: 10,
}

export default function theme(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
